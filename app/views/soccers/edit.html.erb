<h1>Editing soccer</h1>

<%= form_for @soccer, url: soccers_path(@soccer.id), method: :put do |f| %>
    <% if @soccer.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :entry_date %>
      <br>
      <%= f.text_field :entry_date %>
    </div>

    <div class="field">
      <%= f.label :entry_location %>
      <br>
      <%= f.text_field :entry_location %>
    </div>


    <div class="field">
      <%= f.label :entry_duration %>
      <br>
      <%= f.text_field :entry_duration %>
    </div>

    <div class="field">
      <%= f.label :comment %>
      <br>
      <%= f.text_area :comment %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>


<%= link_to 'Show', action: 'show', id: @soccer.id %> |
<%= link_to 'Back', soccers_path %>
