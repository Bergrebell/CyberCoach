<h2>Editing running</h2>

<%= form_for @running, url: running_path(@running.id), method: :put, :html => {:class => 'uk-form uk-form-horizontal'} do |f| %>

    <%= render 'form', :f => f %>

<% end %>
